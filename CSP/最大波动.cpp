#include"iostream"
using namespace std;

/*int abs(int a){
	if (a > 0) return a;
	else return -a;
}

int main(){
	int n;
	int a[10000] = {0};
	int maxx = 0;
	cin >> n;
	for (int i = 0; i < n; i++)
		cin >> a[i];

	for (int i = 1; i < n; i++){
		if (abs(a[i] - a[i-1])>maxx) maxx = abs(a[i] - a[i-1]);
	}
	cout << maxx << endl;
	return 0;
	}*/