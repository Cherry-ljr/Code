#include"iostream"
using namespace std;

/*int main(){
	int n;
	int flag = 0;
	int a[1010] = { 0 };
	cin >> n;
	for (int i = 0; i < n; i++)
		cin >> a[i];

	for (int i = 1; i < n-1; i++){
		if ((a[i + 1]< a[i] && a[i] > a[i - 1]) || (a[i + 1]> a[i] && a[i] < a[i - 1]))
		{
			flag++;
			//cout << a[i] << " " << endl;
		}
	}
	cout << flag << endl;
	return 0;
}*/