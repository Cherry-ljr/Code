#include"iostream"
using namespace std;

/*int abs(int a){
	if (a > 0) return a;
	else return -a;
}
int main(){
	int n = 0;
	int min = 1000;
	int a[1000] = { -1 };
	cin >> n;	
	for (int i = 0; i < n; i++)
		cin >> a[i]; 
	
	for (int j = 0; j < n; j++)
		for (int k = j+1; k < n; k++)
		{
			if (abs(a[k] - a[j]) < min)  min = abs(a[k] - a[j]);
		}
	
	cout << min << endl;
	return 0;
}*/